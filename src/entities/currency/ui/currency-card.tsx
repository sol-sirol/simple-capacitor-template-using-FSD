import { Block } from "framework7-react";

interface CurrencyCardProps {
  id: string;
}

export const CurrencyCard = ({id}: CurrencyCardProps) => {
  return (
    <div>
      <span>

      </span>
    </div>
  );
};
